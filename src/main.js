'use strict';

const LinkedList = require('./lib/linked-list');

const linkedlist = LinkedList();
linkedlist.push(10);
linkedlist.push(12);
linkedlist.push(14);
linkedlist.push('Hooray for Linked Lists!!');

linkedlist.pop();
